{
  "name": "vault-server",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "docker build -t vault-front-homework:latest .",
    "dev": "pnpm build && docker run -p 5000:5000 vault-front-homework:latest"
  }
}